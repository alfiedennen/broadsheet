# pnpm settings
# strict-peer-dependencies: false — Svelte/SvelteKit's plugin ecosystem
# is loose with peer deps; strict mode generates noise without value
strict-peer-dependencies=false
auto-install-peers=true

# Hoist Svelte deps to the workspace root for IDE language server
shamefully-hoist=false
