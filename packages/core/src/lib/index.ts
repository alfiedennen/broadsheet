// Public exports of @broadsheet/core for plugin packages to consume.
// M0 placeholder — real exports populate as M1 (HA client + safety
// rails) and M2 (discovery layer) ship.

export const VERSION = '0.0.1';
