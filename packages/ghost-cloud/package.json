{
  "name": "@broadsheet/ghost-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "The Long Take — radar event playback (water-membrane time-tube) plugin for broadsheet",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'ghost-cloud is built THROUGH @broadsheet/core (core imports it via the plugin registry) — no standalone build' && exit 0"
  },
  "peerDependencies": {
    "@broadsheet/core": "workspace:*",
    "svelte": "^5.1.0"
  }
}
