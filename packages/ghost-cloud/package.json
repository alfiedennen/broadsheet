{
  "name": "@broadsheet/ghost-cloud",
  "version": "0.0.1",
  "private": true,
  "description": "Radar event playback (water-membrane time-tube) plugin for broadsheet",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'ghost-cloud: stub at M0 — implementation lands post-v0.1' && exit 0"
  },
  "peerDependencies": {
    "@broadsheet/core": "workspace:*"
  }
}
