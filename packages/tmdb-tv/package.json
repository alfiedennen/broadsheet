{
  "name": "@broadsheet/tmdb-tv",
  "version": "0.1.0",
  "private": true,
  "description": "TMDB-driven content rows for broadsheet's /tv page",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'tmdb-tv is built THROUGH @broadsheet/core (core imports it via the plugin registry) — no standalone build' && exit 0"
  },
  "peerDependencies": {
    "@broadsheet/core": "workspace:*",
    "svelte": "^5.1.0"
  }
}
